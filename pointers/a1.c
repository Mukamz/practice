#include <stdio.h>

/**
 * main - Entry point
 * Return: Always (0) success
 */

int main(void)
{
	int a, b, c;

	printf("Enter value of a, b and c\n");
	printf("Enter value of a, b and c\n");
	scanf("%d\n, %d\n, %d\n", &a, &b, &c);

	return (0);
}
