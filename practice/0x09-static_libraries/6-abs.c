#include "main.h"

/**
 * _abs- function that computes the absolute value of an integer
 * @n: parameter
 * Return:Absolute value of an integer
 */
int _abs(int n)
{
	if (n < 0)
		n = -n;
	return (n);
}
