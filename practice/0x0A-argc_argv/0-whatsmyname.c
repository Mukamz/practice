#include <stdio.h>

/**
 * main - prints program followed by new line
 * @argc: number of args
 * @argv: array of pointers to the arguement
 * Return: Always 0
 */
int main(int __attribute((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
